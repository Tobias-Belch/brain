---
title: "Mila's Room"
---

import { cm, formatValueWithUnit, m, toM } from "@pocs/values";
import JscadModelViewer from "@jscad/components/JscadModelViewer.astro";
import { MilasRoom, measurements } from "./models/milas-room.model";
import { materials } from "./models/materials";

export const state = {
  bed: {
    variant: "double",
  }
}

<JscadModelViewer
  name="milas-room-model"
  materials={materials}
  model={MilasRoom({ state })}
  background="#0d1117"
  style={{ width: "100%", height: "70vh", marginBottom: "3rem" }}
/>

## Measurements

| | WxHxD |
|-|-------|
| Room | {formatValueWithUnit(measurements.room.width)} x {formatValueWithUnit(measurements.room.height)} x {formatValueWithUnit(measurements.room.depth)} ({(toM(measurements.room.width).value * toM(measurements.room.depth).value).toFixed(1)} m²) |
| Window | {formatValueWithUnit(measurements.room.window.width)} x {formatValueWithUnit(measurements.room.window.height)} x {formatValueWithUnit(measurements.room.window.depth)} |
| Window Board | {formatValueWithUnit(measurements.room.window.board.width)} x {formatValueWithUnit(measurements.room.window.board.height)} x {formatValueWithUnit(measurements.room.window.board.depth)} |
| Door | {formatValueWithUnit(measurements.room.door.width)} x {formatValueWithUnit(measurements.room.door.height)} x {formatValueWithUnit(measurements.room.door.depth)} |